<header class="sub-header">
  <h2>회원정보 수정 <%= resource_name.to_s.humanize %></h2>
</header>

<main>
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="field">
      <%= resource.email %>
    </div>



    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
    <% end %>

    <div class="field">
      <%= f.label :"현재 비밀번호" %> <i>(현재 비밀번호를 정확히 입력해 주세요. )</i><br />
      <%= f.password_field :current_password, autocomplete: "current-password" %>
    </div>

    <div class="field">
      <%= f.label :"새로운 비밀번호" %> <i>(비밀번호를 변경하고 싶지 않다면 입력하지 말고 빈칸으로 남겨두세요)</i><br />
      <%= f.password_field :password, autocomplete: "new-password" %>
      <% if @minimum_password_length %>
        <br />
        <em>최소 <%= @minimum_password_length %>자 이상으로 작성해주세요</em>
      <% end %>
    </div>

    <div class="field">
      <%= f.label :"새로운 비밀번호 확인" %><br />
      <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
    </div>
    
    <div class="field">
      <%= f.label :"인스타그램 계정" %>
      <%= f.text_field :instagram %>
    </div>

    <div class="field">
      <%= f.label :"트위터 계정" %>
      <%= f.text_field :twitter %>
    </div>

    <div class="actions">
      <%= f.submit "Update" %>
    </div>
  <% end %>

  <hr>
  
  <h3>회원 탈퇴</h3>

  <p>Unhappy? <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>

  <%= link_to "Back", :back %>
</main>
