<<<<<<< Updated upstream
blog setting edit
<% url, method = users_path, :post  %>
<% url, method = user_path(@user.slug), :put if action_name == 'edit' %>

<%= form_for(@user, url: url) do |f| %>
  <div>
    프로필사진 추가
    <%= f.file_field :image %>
  </div>

  <span class="ui" style="float: right;">
    <button class="ui button" type="submit">등록</button>
  </span>
  <% end %>
=======
<header class="sub-header">
  <h3>블로그 꾸미기 설정 변경</h3>
</header>

<main>
  <% url, method = users_path, :post  %>
  <% url, method = user_path(@user.slug), :put if action_name == 'edit' %>

  <%= form_for(@user, url: url) do |f| %>

  <table>
    <tr>
      <td>
        <span><strong>프로필사진 추가</strong></span>
      </td>
      <td>
        <%= f.file_field :image, accept: "image/*"%>
      </td>
    </tr>

    <tr>
      <td>
        <span><strong>블로그 소개글 추가</strong></span>
      </td>
      <td>
        <%= f.text_area :intro , class: "intro-text"%>
      </td>
    </tr>
  </table>

    <span style="float: right;">
      <button type="submit">등록</button>
    </span>
  <% end %>

</main>

<style>
  table {
    width: 80%;
    height: 300px;
  }

  .intro-text {
    width: 100%;
    height: 100%;
    overflow: visible;
  }
</style>
>>>>>>> Stashed changes
